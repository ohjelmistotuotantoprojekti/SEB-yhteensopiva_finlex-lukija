version: "2"
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: devdb
    expose:
      - 5432
    volumes:
      - pgdata:/var/lib/postgresql/data
  backend-dev:
    build:
      context: .
    ports:
      - 3001:3001
    environment:
      PG_URI: postgres://postgres:postgres@postgres:5432/devdb
      NODE_ENV: development
      DATABASE_PASSWORD: AAA
      START_YEAR: 2024
    depends_on:
      - postgres

volumes:
  pgdata:
    driver: local